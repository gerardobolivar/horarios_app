import { ActionFunctionArgs } from "@remix-run/node";

const HorarioAction = async ({ params, request }: ActionFunctionArgs) => {
  //const formData = await request.formData();
  return null;
}

export default HorarioAction; 